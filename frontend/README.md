# CRM Frontend (Verification Console)

## Run
1. `cd frontend`
2. `npm install`
3. `npm run dev`

Default URL: `http://localhost:5173`

If backend is not on `http://localhost:8080`, create `.env` with:

```env
VITE_API_BASE_URL=http://your-api-host
```
