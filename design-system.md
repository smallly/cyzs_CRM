# Design System（V1 现行规范）
> 本规范用于统一当前 CRM 项目的页面样式与交互表现。  
> 内容基于当前项目已确认口径，不引入未确认的新视觉体系。

---

## 一、适用范围

- 前端项目：`frontend/src/App.vue` + `frontend/src/style.css`
- 适用对象：列表页、详情页、弹窗、抽屉、表单、表格、提示信息

---

## 二、全局视觉基线

### 1. 基础风格
- 页面基底：浅灰背景 + 白色卡片
- 主要强调色：蓝色（按钮、链接、激活态）
- 文本层级：标题深色、正文中深色、辅助信息浅灰

### 2. 组件圆角与边框
- 输入框、下拉、按钮、卡片统一圆角风格
- 边框统一浅灰细边线
- 避免高对比重边框与多层描边叠加

### 3. 间距
- 使用统一的行距与块间距（当前样式体系内已有 spacing 节奏）
- 避免同一块内混用多个“临时内联间距”

---

## 三、表单规范

### 1. 必填与选填
- 必填字段统一使用 `*` 标识
- 全局不展示“选填”文案

### 2. 字段布局
- 弹窗/抽屉表单默认“一行 2 个字段”
- 特殊字段（如大文本、多选区）可跨列占满
- 入驻阶段弹窗仅展示“入驻日期”字段（除阶段选择外）

### 3. 金额字段命名
- 所有金额类字段名统一带后缀：`（元）`
- 示例：`合同金额（元）`、`回款金额（元）`

### 4. 日期字段
- 跟进时间按“年月日”展示与录入，不显示时分秒

---

## 四、按钮与操作规范

### 1. 主次按钮
- 主按钮文案统一用“保存”（新建/编辑弹窗场景）
- 次按钮文案通常为“取消”

### 2. 删除操作
- 所有删除必须二次确认（弹窗确认）

### 3. 顶部固定操作区
- 在长表单页面中，保存/取消可置于顶部固定区域
- 顶部操作区固定，内容区滚动时不跟随滚动

---

## 五、弹窗与抽屉规范

### 1. 关闭能力
- 所有弹窗右上角必须有关闭 icon
- 支持点击关闭

### 2. 内容与遮罩
- 弹层打开后，背景内容不可“穿透干扰”
- 滚动时不应出现后层内容透出到弹层头部区域

### 3. 标题区
- 标题左侧显示，关闭 icon 右侧显示
- 不使用“关闭”文字按钮替代关闭 icon（除明确要求场景）

---

## 六、Tab 规范

### 1. 容器样式
- Tab 外层为一个整体底色容器
- 激活项使用另一种底色高亮，不做碎片化块状堆叠

### 2. 分割线
- 去掉 Tab 下方多余横线
- 保持容器整体性，不出现无意义细线条

---

## 七、表格规范

### 1. 网格线
- 参考当前确认口径：不使用竖线分割
- 保留必要横向分隔，保持行可读性

### 2. 对齐
- 单元格内容垂直居中
- 项目名称链接左对齐显示

### 3. 链接样式
- 列表链接使用蓝色文字
- 去掉下划线

### 4. 阶段列
- 项目列表“项目阶段”列为只读展示，不在列表页直接修改
- 列宽可适当收窄，避免挤压操作列

---

## 八、详情页信息块规范

- 基础信息区域采用“整块表格化信息展示”
- 去除冗余外框与多余装饰线
- 时间字段默认不加粗（除标题/状态等明确强调项）
- “项目编号”展示不带中文冒号，值使用常规字重

---

## 九、跟进记录卡片规范

### 1. 头部
- 头像图标与后方文字（姓名、创建时间）水平居中对齐
- 创建时间文案不加粗

### 2. 正文与附件
- 正文优先展示跟进内容
- 附件存在时在正文中展示附件缩略/文件块
- 无附件时不展示“未选择文件”等占位文案

### 3. 操作
- 编辑、删除操作风格参考已确认样式（图标化、轻量操作）
- 权限遵循项目权限口径

---

## 十、提示反馈规范

- 全局错误提示统一使用 toast
- toast 展示时长：2 秒
- 不使用页面顶部常驻错误条

---

## 十一、组件优先级（当前阶段）

- 当前项目使用原生 Vue 模板 + 自定义样式实现 UI
- 在未完成 UI 框架迁移前，新增页面需优先复用现有样式类与交互模式
- 禁止同类组件重复造轮子（同页面中避免出现多个不同风格的表格/弹窗/按钮体系）

